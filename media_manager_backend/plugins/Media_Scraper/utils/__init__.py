"""
工具模块

注意：这些工具文件来自旧的 JavSP 项目，可能包含对 javsp 模块的依赖。
目前项目中没有使用这些工具，保留它们是为了将来可能的需要。

如果需要使用这些工具，请先检查并移除对 javsp 的依赖。
"""

# 暂时不导出任何内容，因为这些工具依赖旧的 javsp 模块
__all__ = []

# 将来可以导出的工具函数（需要先移除 javsp 依赖）:
# from .lib import re_escape, resource_path, strftime_to_minutes, detect_special_attr
# from .avid import get_id, get_cid, guess_av_type
